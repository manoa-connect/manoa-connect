import MatchingPage from '@/components/MatchingPage';
import { Col, Container, Image, Row } from 'react-bootstrap';

/** The Home page. */
const Home = () => (
  <main>
  <MatchingPage />

  </main>
);

export default Home;
